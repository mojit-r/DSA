# Big O / Time Complexity

## What is Time Complexity?

Time complexity describes how the amount of work an algorithm
does grows as the input size grows.

We represent the input size as `n`.

## Common Complexities

- O(1) → Constant
- O(log n) → Logarithmic
- O(n) → Linear
- O(n log n) → Linearithmic
- O(n²) → Quadratic
- O(2ⁿ) → Exponential

## Examples

### O(1)

Accessing an element by index:

```dart
numbers[0];